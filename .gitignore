# Python
.venv/
__pycache__/
*.pyc

# Secrets — local dev only; production uses GitHub Secrets. NEVER commit.
.env

# Local working data — audio, transcripts, state. Transcripts belong in the
# PRIVATE repo, never this (future-public) one.
data/
# The private repo's checkout in CI — must never be committed here.
archive/

# Audio anywhere else
*.mp3
*.m4a
*.wav
